# VG Sources

#message("/src/ current CMAKE src dir is " ${CMAKE_CURRENT_SOURCE_DIR})
file(GLOB_RECURSE THE_SOURCE_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp *.c)
file(GLOB_RECURSE THE_HEADER_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.h *.hpp)

# Remove LuaIntf source files (Using header only mode)
set (ITEMS_TO_REMOVE 
${CMAKE_CURRENT_LIST_DIR}/ext/LuaIntf/src/CppBindClass.cpp
)
list(REMOVE_ITEM THE_SOURCE_FILES ${ITEMS_TO_REMOVE})

list(LENGTH THE_SOURCE_FILES THE_SOURCE_FILES_LEN)
list(LENGTH THE_HEADER_FILES THE_HEADER_FILES_LEN)

#debug
message(${THE_HEADER_FILES_LEN} " Headers: " ${THE_HEADER_FILES})
message(${THE_SOURCE_FILES_LEN} " Sources: " ${THE_SOURCE_FILES})
message("")

set(PRJ_SOURCE_FILES ${THE_SOURCE_FILES} PARENT_SCOPE)
set(PRJ_HEADER_FILES ${THE_HEADER_FILES} PARENT_SCOPE)

